/**
 * `@dambi/core/internal` — 대시보드(정책 저작 UI) 전용 표면이 들어올 자리.
 *
 * 지금은 아무것도 export하지 않는다. `renderDef` / `estToPolicyText` 같은
 * 저작-WASM 의존 API는 Phase 2/3에서 런타임 6종 분리와 함께 여기로 온다
 * (phase1-preflight.md:215-227). 그때까지 이 진입점은 자리표시자다.
 */

export {};
